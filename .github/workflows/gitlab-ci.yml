name: Trigger GitLab CI

on:
  push:
    branches: [ main ]   # or your main branch name

jobs:
  trigger-gitlab:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger GitLab pipeline
        run: |
          curl -X POST \
            -F token="glptt-1e41e1ae2055979e2667435273a5022959d06ae3" \
            -F ref=main \
            https://gitlab.com/api/v4/projects/75164909/trigger/pipeline

